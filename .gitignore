# ================================================
# WOODEN HOUSE - GITIGNORE
# ================================================

.claude/

# ── NUNCA SUBIR — Credenciales y datos sensibles ─────────────────
.env
.env.local
.env.production
.env.staging

# Claves Firebase Admin SDK
firebase-service-account.json
*-firebase-adminsdk-*.json
serviceAccountKey.json
firebase-credentials.json

# Certificados y claves
*.pem
*.key
*.p12
secrets.json
credentials.json

# ── BASE DE DATOS ─────────────────────────────────────────────────
database/backups/
*.sql.gz
*.sql.zip
*.dump

# Sí subir (estructura y datos de prueba)
!database/schema.sql
!database/seed.sql

# ── LOGS ─────────────────────────────────────────────────────────
logs/
*.log
firebase-debug.log
firestore-debug.log
ui-debug.log

# ── DEPENDENCIAS ─────────────────────────────────────────────────
node_modules/
firebase/functions/node_modules/
vendor/
composer.phar
composer.lock

# ── UPLOADS DE USUARIOS ──────────────────────────────────────────
public/uploads/*
!public/uploads/.gitkeep

# ── SISTEMA OPERATIVO ─────────────────────────────────────────────
# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.fuse_hidden*
.Trash-*

# ── IDEs Y EDITORES ───────────────────────────────────────────────
.vscode/
*.code-workspace
.history/
.idea/
*.iml
*.sublime-project
*.sublime-workspace

# ── ARCHIVOS TEMPORALES ───────────────────────────────────────────
tmp/
temp/
*.tmp
*.bak
*.backup
*.cache
.cache/
cache/
.phpunit.result.cache
.php_cs.cache

# ── FIREBASE ──────────────────────────────────────────────────────
.firebase/
firebase-emulators/

# ── ARCHIVOS COMPRIMIDOS ──────────────────────────────────────────
*.zip
*.tar
*.tar.gz
*.rar
*.7z

# ── NOTAS Y ARCHIVOS PERSONALES ──────────────────────────────────
NOTAS.md
TODO.md
PERSONAL.md
notes/
sandbox/
draft/

# ── SÍ SUBIR (explícito) ─────────────────────────────────────────
!.env.example
!.htaccess
!public/.htaccess
!database/schema.sql
!database/seed.sql
